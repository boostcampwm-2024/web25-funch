import ToolBar from './ToolBar';

const Header = () => {
  return (
    <header className="h-header fixed left-0 top-0 w-full">
      {/* <Search /> */}
      <ToolBar />
    </header>
  );
};

export default Header;
